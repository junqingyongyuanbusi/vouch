// Package worktree — P1.2 — base/candidate pair via temporary-index snapshot.
//
// See worktree.go: Create/Snapshot keep the user's index untouched and include
// untracked + binary files. Dependency: worktree → os/exec only.
package worktree
