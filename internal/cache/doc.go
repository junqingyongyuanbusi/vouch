// Package cache — P1.3 — BaselineCache: content-addressed base-side probe
// results keyed by (base_ref, probe, env_fingerprint, targets_hash).
// See cache.go; invalidation is by key construction.
package cache
